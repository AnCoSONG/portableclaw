#!/usr/bin/env bash
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
export PATH="$ROOT/runtime:$PATH"
export NODE_PATH="$ROOT/app/node_modules"
exec "$ROOT/runtime/node" "$ROOT/app/node_modules/openclaw/openclaw.mjs" "$@"
